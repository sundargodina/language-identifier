# language-identifier

language identifier using naive bayes and random forest methods.
used count vectorizer to convert the language text phrases into matrix of token counts to build the machine learning model.
used label encoding for converting categorical variable(language column) to numerical values.
